/**
 * AI Layout to Gutenberg Admin JS
 *
 * @package    Image_Layout_To_Gutenberg
 * @subpackage Image_Layout_To_Gutenberg/admin/js
 * @since      0.1.0
 */

(function( $ ) {
	'use strict';

	$(document).ready(function() {
		// Add any custom JavaScript for the admin page here.
		// For example, client-side validation for the upload form,
		// or AJAX interactions if you choose to submit the form that way.

		// console.log('ILG AI Admin JS loaded.');

		// Example: Basic alert on form submit (if not using AJAX)
		// $('form').on('submit', function(e) {
		// if ( $('#ilg_ai_image_upload').get(0).files.length === 0 ) {
		// alert('Please select an image to upload.');
		// e.preventDefault();
		// }
		// });
	});

})( jQuery );
